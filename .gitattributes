# Normalize all text files to LF in the repo (Luau tooling expects LF)
* text=auto eol=lf

# Roblox binary formats must never be line-ending normalized
*.rbxl  binary
*.rbxlx binary
*.rbxm  binary
*.rbxmx binary
